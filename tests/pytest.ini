[pytest]
testpaths = unit integration
pythonpath = ..
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    unit: tests unitaires sans dépendance externe
    integration: tests nécessitant l'application démarrée
