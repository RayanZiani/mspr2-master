# Compose RACINE — siège (frontend + API + Redis)
# Usage : npm start
#
# Simulateurs capteurs (local, hors Docker) :
#   npm run sim:clear
#   npm run sim:start
#
# Broker MQTT ESP32 (optionnel) :
#   docker compose -f docker-compose.iot.yml up -d

include:
  - siege/docker-compose.yml
