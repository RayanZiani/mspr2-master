# Base unique Aiven — même MYSQL_URL que la racine du projet (.env)
# Copie MYSQL_URL depuis le .env racine si besoin.
MYSQL_URL=

# MQTT (ESP32 terrain, optionnel avec docker-compose.iot.yml)
MQTT_BROKER_HOST=localhost
MQTT_BROKER_PORT=1883
