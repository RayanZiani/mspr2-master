# MySQL local pays (utilise par mysql-bresil + api-bresil)
MYSQL_ROOT_PASSWORD=changeme_root
MYSQL_DATABASE=futurekawa_br
MYSQL_USER=futurekawa
MYSQL_PASSWORD=changeme_user

# Optionnel (fallback pour scripts/ensure-env.mjs -> siege/.env)
# DATABASE_URL=mysql://user:password@host:20300/defaultdb?ssl-mode=REQUIRED

# Optionnel (valeurs par defaut deja presentes dans l'API)
MYSQL_HOST=mysql-bresil
MQTT_BROKER_HOST=mosquitto-bresil
MQTT_BROKER_PORT=1883
