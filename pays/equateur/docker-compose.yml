# Les APIs pays ne tournent plus en local Docker.
# Données IoT : scripts/simulate_releves_aiven.py → BDD Aiven (MYSQL_URL racine).
# Déploiement API pays : Render ou docker build manuel si besoin.

services: {}
